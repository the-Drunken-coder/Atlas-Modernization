{
  "name": "@atlas/atlas-core-release",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "format:check": "biome check --linter-enabled=false .",
    "lint": "biome lint .",
    "check": "npm run format:check && npm run lint && npm run build && node --test dist/*.test.js"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.14",
    "@types/node": "^26.6.2",
    "typescript": "^7.0.2"
  }
}
