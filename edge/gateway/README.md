# Atlas Edge Gateway

An Edge Gateway connects field communication methods to Atlas Core. It does not represent an Asset and does not run Asset behavior.

This directory intentionally contains no implementation while the Gateway architecture is being designed. The first ground-hosted MAVLink Asset communicates with Core directly and therefore does not need a Gateway.

Do not add executable code here until the Gateway architecture is decided.
