Local Cloudflare tunnel credential files can live here for manual config-file runs.

Do not commit generated tunnel credentials. JSON and PEM credential files in this
directory are ignored by the repository `.gitignore`.
