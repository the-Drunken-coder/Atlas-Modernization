# Ruff configuration for the Atlas_Core helper scripts.
# CI runs `ruff check` and `ruff format --check` over this directory.
target-version = "py311"
line-length = 120

[lint]
# E/W: pycodestyle, F: pyflakes, I: import sorting, UP: pyupgrade, B: bugbear
select = ["E", "W", "F", "I", "UP", "B"]
