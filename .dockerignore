# Docker builds use the repository root as context so atlas_core can import the
# sibling atlas_protocol module. Keep the context narrow.
*
!atlas_core/
!atlas_core/**
!atlas_protocol/
!atlas_protocol/**
