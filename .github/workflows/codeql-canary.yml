name: CodeQL Canary

# Intentionally vulnerable CodeQL canary. DO NOT MERGE.
on:
  issue_comment:
    types:
      - created

permissions: {}

jobs:
  canary:
    runs-on: ubuntu-latest
    steps:
      - name: Echo untrusted comment
        run: echo '${{ github.event.comment.body }}'
