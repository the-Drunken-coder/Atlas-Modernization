1. **Time & Date:** 2026-09-18T15:00:56Z
2. **Name:** MapConsole.tsx is the single termination point for every feature hook, with 17-prop MapView drilling and 6 capability-checked optional data-source methods
3. **Issue:** All feature hooks, cross-feature interaction state (camera sequence, preview targets, drawing suppression), and every optional data-source capability check converge in one 954-line component that hands 17 props (plus a 6-callback spatial bundle) down to MapView, so no layer below it can be composed or reasoned about independently of this file.
4. **Severity:** S4
5. **Location:** surfaces/command-interface/src/features/MapConsole.tsx (954 lines); surfaces/command-interface/src/ui/map/view/MapView.tsx:57-86; surfaces/command-interface/src/atlas/data-source.ts:36-51; surfaces/command-interface/src/state/atlas-context.tsx:163-256
6. **Expected:** MapConsole should coordinate a small number of well-scoped concerns; the remaining cross-feature interaction state (spatial preview target, camera sequencing, mutual suppression during drawing) should live in its own reducer, and the data-source interface should declare capabilities as required members (with fakes implementing them) instead of the UI feature-checking 6 optional methods at every call site.
7. **Actual:** MapConsole.tsx (954 lines) instantiates 34 React hooks, including 8 custom feature hooks (useAtlas, useHeartbeatClock, useSpatialOperationRunner, useMovementHistory, useCommandFlow, useGeometryEdit, useGeofeatureCreate, useGeofeatureDelete) plus inline cross-feature glue (cameraSequenceRef at :98, spatial-preview effect at :101, sidebarRef mirror at :131), and assembles MapView's 17-prop contract inline in JSX, including ternary-conditional callbacks such as `onCancelTask={cancelTask ? (taskId) => cancelTask({ taskId }) : undefined}` at :573.
8. **Reproduction:** Source trace: `wc -l` on MapConsole.tsx = 954. Hook census at component level: 7 useMemo (:282, :326, :351, :359, :363, :367, :368), 7 useCallback (:110, :114, :117, :124, :369, :407, :420), 5 useEffect (:101, :158, :225, :341, :399), 4 useRef (:98, :131, :149, :291), 2 useState (:92, :93), 1 useReducer (:91) = 26 built-in; 8 custom (:87, :90, :99, :137, :294, :305, :306, :309) = 34 total. MapViewProps at MapView.tsx:57-75 lists exactly 17 members (movement, sources, styleId, style, mapSourceOptions, selectedId, editing, drawing, initialCenter, focusTarget, placeDetailTarget, cameraCommand, spatial, onSelectEntity, onMapContextMenu, onBackgroundClick, onStyleSwitchError); the MapSpatialInteraction bundle (MapView.tsx:77-86) carries 6 callbacks (onAreaChange, onDrawingComplete, onCancelDrawing, onViewportArea, onSelectFeature, onBoxZoomActiveChange). AtlasDataSource (atlas/data-source.ts:36-51) declares 6 optional members (movement?, loadEntityDetails?, cancelTask?, canDeleteGeofeature?, deleteGeofeature?, health?), all of which the UI/state layer feature-checks: MapConsole.tsx:89+573 (cancelTask ternary), :152-156+159+182+223 (loadEntityDetails in commandDetailsRequired and effect deps), :137 (atlas.movement, with the throw inside use-movement-history.ts:78), :309 (deleteGeofeature/canDeleteGeofeature, combined at use-geofeature-delete.ts:33), and atlas-context.tsx:163-165 (health guard), :196-197 (loadEntityDetails throw), :203-213 (movement optional-chained ×3), :231 (conditional exposure), :237-241 (cancelTask throw), :249-250 (canDeleteGeofeature `?? true` default), :255-256 (deleteGeofeature throw).
9. **Notes:** Exact counts: 954 lines; 34 hooks (not ~35, and not the ~50 use* occurrences claimed by the other review — 34 is the precise component-level count); 17 MapView props (the "18" variant is wrong); MapSpatialInteraction has 6 callbacks (not 7); 6 optional data-source methods, every one guarded somewhere in UI/state code (inventory above). The finding's file path needs correction: the interface lives at src/atlas/data-source.ts:36-51, not src/data-source.ts. Feature hooks are separately tested: use-command-flow.test.tsx, use-geofeature-create.test.tsx, use-geofeature-delete.test.tsx, use-geometry-edit.test.tsx, use-movement-history.test.tsx, use-spatial-operation-runner.test.tsx (useHeartbeatClock.ts has no dedicated test but is trivial). ui/map/ is indeed layered into interaction/, rendering/, runtime/, view/, each with its own test files (e.g. interaction/map-camera.test.ts, rendering/map-editing.test.tsx, runtime/runtime-loaders.test.tsx, view/MapView.camera.test.tsx) — the downstream layers are well-built and the fault is confined to the single component where everything terminates. This report overlaps docs/problems/2026-09-18-thin-manifest-events.md only at the ~112-line details-fetch state machine (MapConsole.tsx:144-255: state at :144-151, effect :158-223, effect :225-255); that report owns the event-contract root cause and fix directions — fixing it removes the worst single chunk, after which the cross-feature interaction state (preview targets, camera sequence, drawing suppression) is the remaining candidate for its own reducer, which is this report's subject. The optional-methods cleanup (make the 6 members required, have test fakes implement them) is independent of the thin-manifest fix and would delete the guard ladder in atlas-context.tsx:196-256 and the ternary at MapConsole.tsx:573.
