1. **Time & Date:** 2026-09-18T15:00:56Z
2. **Name:** Command catalog is empty; tasking surface (~1,460 Core lines + SDK + UI) never exercised by a real Command
3. **Issue:** `packages/protocol/generated/command_catalog.json` is `[]` and the embedded Go constant is `"[]\n"`, so every running Core serves an empty Command Catalog and the UI permanently shows "No Commands are defined in Atlas Protocol". No namespace definition file exists anywhere (`packages/protocol/commands/` contains only a README), and both plugins are `lifecycle: query_only` with no Command definitions. All Task/Runtime machinery has only ever run against test fixture Commands injected by a compile-time overlay that the acceptance docs themselves say proves nothing about the production catalog.
4. **Severity:** S4 (Minor) — deliberate, documented empty state, not an outage; but a large unproven design surface and a planning gate before more tasking features.
5. **Location:** `packages/protocol/generated/command_catalog.json`, `packages/protocol/generated/go/atlasprotocol/command_catalog.go:6`, `packages/protocol/commands/` (README only), `services/core/internal/actions/task_actions.go` + `task_create.go` + `task_runtime.go` + `task_transition.go` (1,460 lines total), `services/core/cmd/atlas_core/main.go:161,207`, `services/core/internal/integration/request_conformance_test.go:27-33`, `packages/sdk/src/client.ts:200-270`, `surfaces/command-interface/src/features/commands/command-empty-label.ts:8`
6. **Expected:** At least one real operator-facing Command defined in `packages/protocol/commands/` and shipped in the production catalog, with the asset → Core → UI path exercised end-to-end against that catalog before the tasking surface grows further.
7. **Actual:** The production catalog contains zero Commands. Core builds `TaskActions` from the embedded empty catalog (`NewTaskActionsWithPlugins` unmarshals `protocol.CommandCatalogJSON`), so no Task can be created through any real Core. The only end-to-end exercise (SDK task acceptance, simulation acceptance) compiles Core with a temporary Go `-overlay` replacing the catalog constant with `packages/protocol/conformance/tasking/fixtures/catalog.json` (`fixture.immediate`, `fixture.queued`); `tests/acceptance/sdk-tasks.md` states explicitly that this "does not establish that the production catalog contains Task Commands". The external integration conformance server skips Task/Runtime request shapes entirely because its catalog is empty.
8. **Reproduction:** `cat packages/protocol/generated/command_catalog.json` → `[]`; start dev Core and `GET /commands/catalog` returns `[]` (`services/core/internal/api/handlers/handler_command_catalog.go` serves the embedded constant); `ls packages/protocol/commands/` shows no namespace JSON; `grep -rn "createTask" tests/acceptance/simulations/moving-assets.mjs` shows tasking only inside the `createTaskBeforeCleanup` overlay section.
9. **Notes:** Disproving evidence checked: catalog is populated only from `packages/protocol/commands/*.json` (`packages/protocol/tools/internal/artifacts/command_catalog.go` `loadCommandDefinitions`); no plugin provides a Command (`plugins/building_scan`, `plugins/reference` manifests are `query_only` with no command entries). The machinery is well covered at the unit/integration level via the fixture catalog (`task_state_test.go`, `task_lifecycle_test.go`, `task_runtime_benchmark_test.go` use `NewTaskActionsWithCatalog`) and even acceptance-tested end-to-end — but always with the overlay, never a production Command. `packages/protocol/commands/README.md` documents the current empty state as expected. Risk item, not a defect: treat "prove one real Command end-to-end" as the gate before adding more tasking features.
