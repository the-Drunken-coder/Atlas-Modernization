1. **Time & Date:** 2026-09-19T00:30:00Z
2. **Name:** Production flight catalog is present; browser → Core → host → aircraft journey still unproven as one run
3. **Issue:** The production catalog now contains `flight.takeoff`, `flight.goto`, `flight.return_to_launch`, and `flight.land` (`packages/protocol/commands/flight.json`). Core, purpose-built interface forms, and the Asset host are implemented and tested per layer. The remaining gap is a single journey through the actual Command Interface, disposable Core, the real Asset process, and an aircraft (SITL or hardware). Existing SDK/browser Task acceptance still uses fixture-catalog overlays.
4. **Severity:** S4 (Minor) — production Commands exist; the original empty-catalog gate is closed. Full operator-to-aircraft proof is still outstanding, as is macOS serial / real SiK / Safari coverage.
5. **Location:** `packages/protocol/generated/command_catalog.json`, `edge/asset/`, `surfaces/command-interface/src/features/commands/flight-inputs.tsx`, `tests/acceptance/sdk-tasks.mjs`, `tests/acceptance/browser/commands/`
6. **Expected:** One primary acceptance journey through the actual Command Interface, real disposable Core, public SDK, real Asset process, and ArduCopter software-in-the-loop (or recorded hardware) against the production catalog.
7. **Actual:** Protocol, Core supersession, interface forms, and host↔vehicle MAVLink are exercised separately. SDK Task and browser Command acceptance still overlay fixture Commands. Hardware Mac/SiK and Safari remain unrecorded.
8. **Reproduction:** `cat packages/protocol/generated/command_catalog.json` lists the four `flight.*` Commands. `npm run check --workspace @the-drunken-coder/atlas-asset` covers host↔fake ArduCopter over real MAVLink bytes. No `tests/acceptance` script currently drives production flight forms through Core to the Asset host.
9. **Notes:** Leave this report until the production-catalog operator journey is observed together, or until hardware/Safari verification is recorded as a separate outstanding item and this report is retired.
