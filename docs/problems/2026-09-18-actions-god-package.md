1. **Time & Date:** 2026-09-18T15:00:56Z
2. **Name:** internal/actions is a 6,400-line god package with no internal boundaries; feed dispatcher already reaches through it to run a retention query
3. **Issue:** `services/core/internal/actions` holds 33 non-test files / 6,377 lines spanning four unrelated concerns (domain orchestration, generic cursor/pagination infrastructure, JSON-blob contract machinery, change-log writer). Being one Go package, every file can reach every other file's symbols — nothing constrains coupling — and the leak is not hypothetical: `internal/feed/dispatcher.go:85` constructs an `actions.NewEntityActions(d.pool)` solely to call the retention method `PruneMovement` (movement_history.go:258), which uses only the pool, not any entity-orchestration state.
4. **Severity:** S4
5. **Location:** services/core/internal/actions/ (whole package); services/core/internal/feed/dispatcher.go:85; services/core/internal/actions/movement_history.go:258
6. **Expected:** Each concern lives in its own package with an explicit API surface, so feed retention code depends on a narrow retention/changelog package rather than instantiating a domain orchestrator, and cross-concern reachability is compiler-enforced.
7. **Actual:** All four concerns share one package namespace. The feed dispatcher must import the domain `actions` package and build a throwaway `EntityActions` to reach a one-line retention DELETE; handlers (13 files across `internal/api/handlers` plus `cmd/atlas_core/main.go` and `internal/feed/dispatcher.go`) import `internal/actions` and pull an undifferentiated mix of domain types, error types, pagination helpers (`ClampListLimit`, `FullDatasetLimits`), and changelog symbols (`PruneChangeRecords`, `ReadChangeRecords`, `ChangeRecordRetention`) from the single import.
8. **Reproduction:**
   - `ls services/core/internal/actions | grep -cv _test.go` → 33; `wc -l` of those files → 6,377 total (matches the finding).
   - Named seam files total 857 lines: query_cursor.go (183), cursor_list_page.go (78), query_rows.go (145), query_resources.go (60), change_hook.go (247), write_version.go (54), change_retention.go (90).
   - dispatcher.go:85: `err := actions.NewEntityActions(d.pool).PruneMovement(pruneCtx)` — `PruneMovement` (movement_history.go:258) only uses `a.pool.Exec` against `entity_movement_samples`; no entity orchestration involved.
   - Importer census (`grep -rn "internal/actions" --include='*.go'` excluding the package itself): `cmd/atlas_core/main.go:18`, `internal/feed/dispatcher.go:12`, and 11 handler files under `internal/api/handlers/`. Handlers consume domain actions *and* generic infra (e.g. `actions.ClampListLimit`, `actions.CursorExpiredError`, `actions.FullDatasetLimits` in handler_query_parsing.go/handler_response.go) from the same import, showing package-level access is unconstrained.
   - JSON-blob contract machinery confirmed: json_blob_contracts.go (179 lines) defines `patchValidatedJSONBlob`, `jsonBlobFieldFilter`, promoted-field filters `entityPromotedBlobFields`/`objectPromotedBlobFields` (lines 35–44), merge/remove helpers for promoted vs extra keys; object_json_contracts.go wires it for objects; entity_actions.go:318–326 (`patchEntityJSON`) consumes it, validating nested JSON blobs (components/extra, `ValidateEntityBlob`). validation.go/component_validation.go complete the blob-contract cluster.
   - Cross-concern reachability inside the package (same package = no import needed, no constraint): domain files call pagination infra (entity_actions.go, object_actions.go, object_reference_queries.go, query_actions.go, query_actions_types.go, task_actions.go use `readCursorListPage`/cursor encoders/paged-row openers) and changelog infra (entity_actions.go, object_actions.go, object_upload.go, query_actions.go, task_actions.go, task_create.go, task_runtime.go, task_transition.go use `RecordResourceChange`/`beginChangeTx`/`nextChangeVersion`).
9. **Notes:** Investigation hints for a future fix (remedy is a suggestion — the confirmed problem is the missing boundary and the dispatcher leak, not any specific split): (a) The two proposed seams are largely real. Changelog seam (change_hook.go + write_version.go + change_retention.go, 391 lines) references no domain orchestrator symbols (`EntityActions`/`ObjectActions`/`TaskActions`/`QueryActions` absent) and depends only on `models`, `protocol`, and pgx — extraction looks clean. Query seam (query_cursor.go + cursor_list_page.go + query_rows.go + query_resources.go, 466 lines) is also free of domain orchestrator symbols, but query_resources.go defines methods on `resourceQuerySpec[T]`, whose concrete entity/task/object instances (with `entitySelectSQL`, `scanEntity`, etc.) live in the domain file query_collect.go:20–40 — that generic spec type would need to move into internal/query or be parameterized. (b) `ListPage[T]` (pagination.go, the shared result type) sits in the actions package and is consumed by handlers via the `actions.` prefix; it belongs with the query seam. (c) JSON-blob contract machinery (json_blob_contracts.go, object_json_contracts.go, validation.go, component_validation.go — ~369 lines) is a third extractable cluster centered on promoted-field JSON merge/validation. (d) The dispatcher leak has two shapes: `PruneMovement` (entity-orchestrator-shaped API over a pool-only query) and `PruneChangeRecords`/`ReadChangeRecords`/`ChangeRecordRetention` (already free functions, would move with the changelog seam). A retention-facing API on the changelog/retention package would eliminate the `NewEntityActions` construction at dispatcher.go:85. (e) Not a runtime defect; no user-visible failure. Treat as structural debt for the next core refactor.
