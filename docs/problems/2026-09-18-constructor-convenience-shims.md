1. **Time & Date:** 2026-09-18T15:00:56Z
2. **Name:** Constructor convenience variants in Core handlers/actions are mostly test-only dead weight, and production builds two TaskActions instances
3. **Issue:** Layered constructor variants carry no production callers, and main.go's reconciler TaskActions duplicates the one the Handler builds internally.
4. **Severity:** S4
5. **Location:** services/core/internal/api/handlers/handlers.go:31-53; services/core/internal/actions/task_actions.go:29-50; services/core/cmd/atlas_core/main.go:157-161,223
6. **Expected:** One constructor path per type used by production; test-only fixture needs served by explicit test helpers. One TaskActions (and its pluginIDs derivation) per process.
7. **Actual:** Three NewHandler variants and four NewTaskActions variants exist where only one variant of each is used in production. main.go builds a TaskActions at line 161 for the timeout reconciler while handlers.NewHandlerWithPlugins (handlers.go:53) builds a second TaskActions over the same pool from the same cfg.Plugins.
8. **Reproduction:** `grep -rn "<name>" --include='*.go' services/core` per variant (excluding the definitions themselves).
9. **Notes:**
   - Caller inventory (production = non-test files, excluding variant-to-variant delegation):
     - `NewHandler` (handlers.go:31): test-only — handler_movement_test.go:18, handler_task_lifecycle_integration_test.go:24, handler_setup_test.go:20,27,30. No production caller.
     - `NewHandlerWithFeed` (handlers.go:36): test-only — handler_feed_integration_test.go:51,190. No production caller.
     - `NewHandlerWithPlugins` (handlers.go:41): production (main.go:223) plus delegation from the two no-production-caller variants above.
     - `NewTaskActions` (task_actions.go:29): no production caller; one trivial test use — task_state_test.go:212 (`NewTaskActions(nil) == nil`-style smoke check).
     - `NewTaskActionsWithPlugins` (task_actions.go:33): production (main.go:161, handlers.go:53); also test use in tool_assets_test.go:87.
     - `NewTaskActionsWithCatalog` (task_actions.go:44): no production caller; used broadly by tests (task_state_test.go:17,211,260; task_lifecycle_test.go:63,92,266,339,471,564,627,678; task_runtime_benchmark_test.go:32). Its doc comment states it "exists so shared conformance fixtures can exercise behavior while the production catalog remains empty" — a deliberately documented test seam, not an accidental shim.
     - `NewTaskActionsWithCatalogAndPlugins` (task_actions.go:48): no production caller; single test use — tool_assets_test.go:27.
   - Two-instances nuance: the review's phrasing "main.go builds TaskActions twice" is imprecise — main.go:161 calls the constructor once, and the second instance is built invisibly inside handlers.NewHandlerWithPlugins at handlers.go:53. Both instances derive pluginIDs from the same `cfg.Plugins` (main.go:157-159 vs handlers.go:48-51) over the same `db.Pool`. TaskActions holds only pool, a static catalog map (decoded from generated `protocol.CommandCatalogJSON`), and `pluginAssets` derived from pluginIDs — so the two structs are value-identical at build time and share the pool, meaning no real state-divergence risk today (no mutable fields, no separate transactions). The cost is duplicated construction and a hidden second instance, i.e. a duplicated path, not a correctness bug. (EntityActions has the same shape: main.go's reconciler doesn't use one, but handlers.go:52 also derives pluginIDs independently.)
   - AGENTS.md wording check: AGENTS.md does not literally say "avoid convenience shims." The relevant lines are: "Prefer the simplest correct long-term design over compatibility shims, duplicated paths, or preserving an awkward implementation." (line 7) and "Prefer a direct expression, guard clause, or call when it is clearer than a helper. Split code only when it improves readability, error handling, or reuse that already exists." (line 13). The finding's substance — layering of unused constructor variants as duplicated paths — matches line 7; the review paraphrased.
