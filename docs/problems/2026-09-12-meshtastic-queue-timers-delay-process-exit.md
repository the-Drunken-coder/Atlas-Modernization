1. **Time & Date:** 2026-09-16T15:42:34Z
2. **Name:** Meshtastic queue timers delay normal Link process exit
3. **Issue:** After `startLinkService.close()` finishes, Link processes that used the real `MeshtasticSerialRadio` adapter remain alive until referenced packet-queue timers expire.
4. **Severity:** S3 (Moderate)
5. **Location:** `@meshtastic/core` 2.6.7 `dist/mod.js:12545-12599,13309-13319`, pinned by `packages/meshtastic-link/package.json:35`; the normal-exit assertion is enforced by `packages/meshtastic-link/acceptance/process-acceptance.ts:627-663`, and the runner records post-close resources in `packages/meshtastic-link/acceptance/process-runner.ts:92-131`
6. **Expected:** A `SIGTERM` requests the shared lifecycle's ordered cleanup, both compiled Link processes exit normally within the acceptance's 15-second deadline, the event stream closes, and their loopback ports become reusable.
7. **Actual:** Configuration, authenticated joining, public state delivery, and Shared Picture publication succeed. Both process summaries report zero lifecycle-owned active connections and pending writes. After lifecycle close and IPC disconnect, however, each process still reports 11 active `Timeout` resources and misses the normal-exit deadline. The controller's cleanup signal terminates the processes, so it cannot claim normal exit or port-reuse evidence. In `@meshtastic/core` 2.6.7, `Queue.push` schedules a referenced 60-second `setTimeout` for every packet; acknowledgement removes the queue item without cancelling its timer, and `disconnect()` clears queue entries without cancelling those timers.
8. **Reproduction:**
   1. Check out clean revision `798c2e066f6874299336794335367b86a907a090` and use Node 24.
   2. Run `npm run test:acceptance --workspace @the-drunken-coder/atlas-meshtastic-link`.
   3. In scheduled CI run [35074943404](https://github.com/the-Drunken-coder/Atlas-Modernization/actions/runs/35074943404), observe the `join-rejected-gateway` process miss the 15-second normal-exit bound in both acceptance seeds.
   4. In the preserved focused reproduction from the same acceptance family, inspect `gateway.summary.json` and `asset.summary.json` under `.tmp/link-acceptance/baseline-36b22ea7-f8f6-473e-ab3e-96538fc7d913`; each records 11 `Timeout` resources after IPC disconnect while `lifecycle_cleanup.active_connections` and `pending_writes` are zero.
9. **Notes:** The current checkout still pins `@meshtastic/core` 2.6.7 and its production `MeshtasticSerialRadio.close()` rejects local pending sends, disconnects the transport, and disconnects `MeshDevice`; it has no access to the dependency's packet-queue timers. The dependency source trace shows `Queue.push` schedules a referenced 60-second `setTimeout` per packet, while acknowledgement and `disconnect()` clear queue state without cancelling those handles. Preserved focused evidence is at `/tmp/atlas-testing-coordination/evidence-389-shutdown-confirmed`, including checksums and exact stdout. Issue #389 keeps the normal-exit assertion active. Do not add `process.exit`, unref the observed timers, or weaken the deadline in the test-only PR; repair or update the production dependency lifecycle separately. The live CI run was not downloadable from this environment, so the current two-seed run ID is retained from the supplied CI finding while the local focused evidence and source trace provide the independent confirmation.
