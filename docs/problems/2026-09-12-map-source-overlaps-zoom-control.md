1. **Time & Date:** 2026-09-12T19:57:00Z
2. **Name:** Map source selector blocks the native zoom-in control
3. **Issue:** The built Command Interface renders the map-source selector over MapLibre's top-right navigation controls, so a normal pointer click cannot activate the visible zoom-in button.
4. **Severity:** S2 (Major)
5. **Location:** `surfaces/command-interface/src/ui/map/view/MapView.tsx:286`, `surfaces/command-interface/src/features/MapConsole.tsx:649-657`, `surfaces/command-interface/src/ui/map/MapSourcePicker.tsx:248-265`, `surfaces/command-interface/src/ui/styles/map.css:739-764`
6. **Expected:** An operator can activate the visible native MapLibre zoom-in button with a normal pointer click.
7. **Actual:** Playwright resolves a visible, enabled, stable `.maplibregl-ctrl-zoom-in`, but the `Map` label inside `.map-overlay-tr.map-source-control` intercepts every pointer attempt. `MapView` adds the native navigation control at `top-right`, while `MapConsole` renders `MapSourcePicker` as an overlay whose CSS also fixes it to the top-right at a higher stacking level.
8. **Reproduction:**
   1. Check out the published implementation commit `90396b7ae75a815a4cadd89427eed307caa4e116` with a clean working tree.
   2. With Node 24 and Docker running, execute `npm run build:sdk && ATLAS_ACCEPTANCE_RUN_LABEL=reproduce-382-90396b7a node tests/acceptance/browser/map-windows.mjs --browser=chromium`.
   3. Sign in through the automated journey, open the Map window fixture Plugin operation, and let the normal click target `.maplibregl-ctrl-zoom-in`.
   4. Observe the click time out because `<label class="map-source-control__label">Map</label>` intercepts pointer events.
9. **Notes:** The first clean-SHA failure at `3bf7b11737e8994a2875a8651d799ed6a8ff8a15` took 57.109 seconds. Its screenshot, trace, HTML, result, requests, and logs are under `.atlas/acceptance/browser-map-windows-chromium/browser-map-windows-chromium-local-mapwin-chromium-ec300a5e-706c-43db-8a9b-e467e8a824d9/`. The historical pre-rebase implementation-SHA recheck at `f53426a0212d53095e947415dd48ead5a0dd5a06` took 50.164 seconds and reproduced the same pointer interception after the real Core Plugin fixture and all independent single-window interactions passed; its artifacts are under `.atlas/acceptance/browser-map-windows-chromium/browser-map-windows-chromium-resume-382-f53426a0-17c139f6-29a7-4301-b976-f7bf4123b36b/`. The equivalent published rebased source is `90396b7ae75a815a4cadd89427eed307caa4e116`. A review recheck at clean `73b0f30ed2b9cdc63d98714e49069dcb1792b94f` took 63.995 seconds; it proved the collapsed close button visible, enabled, within the workspace, and able to remove the window, then failed the preserved zoom assertion with both `click_completed` and `observable_map_change` false and tile requests unchanged at 16. Its artifacts are under `.atlas/acceptance/browser-map-windows-chromium/browser-map-windows-chromium-review-382-73b0f30e-d5670cf0-216c-41b5-b10a-a9297a7a8c9c/`. The tightened review run at clean local commit `c21f48bc469ec8668927e4e08481896d89d72251` took 46.162 seconds and reached the same final product failure after all independent checks passed. It validated the projection-grounded 36 by 36 area through the real Core POST, the exact result row and title, pointer drag-away detachment, full resized-handle containment, restored focus, and the visible, enabled collapsed close button's removal of the window. Its artifacts are under `.atlas/acceptance/browser-map-windows-chromium/browser-map-windows-chromium-review382-c21f48bc-17a4bc02-e26a-4bf9-9a21-9c3fe818c415/`. A direct fixture HTTP probe accepted the projected request and rejected translated, doubled, and stale-low-zoom bodies with status 422. Keep the normal pointer assertion active until a separate product repair makes both top-right controls reachable. The current assertion requires both a completed pointer click and an increase in the highest MapTiler `/z/x/y` tile zoom level, so unrelated late requests at the existing zoom cannot satisfy it. The same visible button's keyboard interaction exercises independent later window behavior without satisfying the pointer requirement.
