# Resource Write Concurrency

1. **Time & Date:** 2026-06-10, America/New_York (updated 2026-08-19)
2. **Name:** Optional strong If-Match for resource writes
3. **Context:** Entity writes previously serialized database rows but did not expose optimistic concurrency to API clients, while object updates did. That made stale writes silent and made check-in/telemetry behavior look like an accidental asymmetry. Tasks now have explicit lifecycle operations rather than general updates.
4. **Decision:** Entity and object update paths accept optional strong `If-Match` headers in the form `"vN"`, backed by each row's `version`. Handlers parse the header to an expected integer version and actions compare that version after acquiring the row lock. Single-resource entity, Task, and object responses return a strong `ETag` derived from the current version. Task clients use that ETag to order lifecycle responses against feed events; lifecycle transitions themselves are validated by Core and do not accept `If-Match`.
5. **Alternatives considered:** Require `If-Match` on every write; rejected because high-frequency and greenfield workflows can still use serialized last-writer-wins when they do not need optimistic protection. Keep object-only `If-Match`; rejected because the concurrency contract should be consistent across resource types. Pass raw headers into actions; rejected because HTTP syntax belongs at the handler boundary.
6. **Consequences:** Entity and object requests without `If-Match` remain last-writer-wins after row serialization. Weak, malformed, or conflicting `If-Match` tokens fail with HTTP 412. The database `version` remains the shared resource version source for ETags and changed-since synchronization, even when a domain resource such as Task does not expose generic metadata in its JSON shape.
7. **Location:** `services/core/internal/api/handlers/handler_preconditions.go`, `services/core/internal/api/handlers/handler_entity.go`, `services/core/internal/api/handlers/handler_task.go`, `services/core/internal/api/handlers/handler_object.go`, `services/core/internal/actions/entity_actions.go`, `services/core/internal/actions/task_actions.go`, `services/core/internal/actions/object_actions.go`, `services/core/internal/serializers/serializers.go`, `packages/sdk/src/http.ts`, `packages/sdk/src/sync-engine.ts`
