# ==============================================================================
# Atlas Modernization
# Go service (services/core/) with Python tooling under services/core/scripts/.
# This lives at the repo root and applies to the entire tree.
# ==============================================================================

# --- OS / editor noise ---
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
*.swp
*.swo
*~
.idea/
.vscode/

# --- Secrets & local config (never commit) ---
.env
.env.*
!.env.example
atlas_core.settings.json
!atlas_core.settings.json.example
docker-compose.override.yml
# Cloudflare tunnel credentials
**/cloudflared/credentials/*.json
**/cloudflared/credentials/*.pem

# --- Go ---
# Compiled objects / platform binaries
*.exe
*.exe~
*.dll
*.so
*.dylib
# Built service binary (anchored so cmd/services/core/ source is kept)
/services/core/atlas_core
# Test binaries and coverage output
*.test
*.out
coverage.html
# Go workspace files (machine-specific)
go.work
go.work.sum
# Vendored dependencies (we use modules, not vendoring)
vendor/

# --- JavaScript / TypeScript SDK ---
node_modules/
coverage/
/packages/sdk/dist/
/packages/plugin-runtime/dist/
/packages/fieldlink/dist/
/surfaces/core-cli/dist/
/plugins/*/dist/
/surfaces/command-interface/dist/
/surfaces/command-interface/storybook-static/
/surfaces/command-interface/.wrangler/
/surfaces/command-interface/.dev.vars*
/surfaces/command-interface/worker-configuration.d.ts
/simulations/dist/
/simulations/.atlas-simulations/

# --- Python tooling (services/core/scripts/) ---
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/
ENV/
.pytest_cache/
.coverage
.coverage.*
htmlcov/
*.egg-info/

# --- Logs / temp ---
logs/
*.log
*.tmp
*.temp

# --- Agent / personal tooling ---
.claude/
.benchmarks/
